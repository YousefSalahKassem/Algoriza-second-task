import 'dart:ui';

import '../styles/colors.dart';

const String columnTitle = 'title';

const String columnDate = 'date';

const String columnStatus = 'status';

const String columnStart='start';

const String columnEnd='end';

const String columnFavourite='favourite';

const String allTasks = 'allTasks';

const String columnReminder = 'reminder';

const String columnRepeat = 'repeat';

List<Color> colors = [red , blue , green, yellow, orange];

List<String> timeSelected = ["1 day before","1 hour before", "30 minutes before", "10 minutes before","never"];
